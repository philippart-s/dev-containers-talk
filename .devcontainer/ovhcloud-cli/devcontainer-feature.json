{
  "name": "OVHcloud CLI",
  "id": "ovhcloud-cli",
  "version": "1.0.0",
  "description": "A feature to install OVHcloud CLI",
  "options": {
    "distribution": {
      "type": "string",
      "enum": [
        "arm",
        "amd"
      ],
      "default": "arm",
      "description": "Choose your distribution type: arm or amd."
    }
  }
}